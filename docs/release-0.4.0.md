# Scriblism 0.4.0

Markdown 的「即時排版」改為原生預覽與原始碼兩種模式。預覽中的文字不能直接輸入；點選區塊會切到原始碼並定位到該處，按 Ctrl+E 可切回預覽。

預覽現在支援表格。表格和正文位於同一個捲動區，寬度一致，儲存格內容會換行。Markdown 原文仍是儲存格式。

下載 `Scriblism.Setup.exe` 安裝或更新。支援 Windows 11 24H2（build 26100）以上、x64；安裝檔尚未簽章。驗證：76 項核心測試、68 項原生介面檢查通過。SHA-256 見 `SHA256SUMS.txt`。
